import type { Config } from 'tailwindcss'

export default {
    theme: {
        extend: {
            colors: {
                accent: '#6366f1',
                'accent-light': '#e0e7ff',
            }
        }
    }
} satisfies Config